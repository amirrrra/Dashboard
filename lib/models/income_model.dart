import 'package:flutter/material.dart';

class IncomeModel {
  final String title, percentage;
  final Color color;

  const IncomeModel({
    required this.title,
    required this.percentage,
    required this.color,
  });
}
