class ExpensesModel {
  final String title, history, image, price;

  const ExpensesModel({
    required this.title,
    required this.history,
    required this.image,
    required this.price,
  });
}
