class ExpensesModel {
  final String title, history, price,image;

  const ExpensesModel({
    required this.title,
    required this.history,
    required this.image,
    required this.price,
  });
}
